import { Container, Typography } from "@mui/material";

export const WalletDashboard = () => {
  return (
    <Container>
      <Typography>Welcome to Wallet App</Typography>
    </Container>
  );
};
